from Code.Data.Text.context import Context


class GraphConstructor:

    """
    a graph constructor takes in a context and possibly other info,
    and uses heuristic rules to extract nodes and relationships
    """

    def __init__(self, context: Context):
        pass