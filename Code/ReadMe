Todo:


* Graph embedder
     * edge feature encodings. an embedder for subtype info
     * more sophisticated attentive summarisation, possibly utilising sliding window att to pretransform a sequence
        before using self attentive pooling

* Graph construction
     * node filters to allow for word nodes be filtered by query words
     * AMR

* GNNs
    * switch additive attention for dot-product

* config summary graphs for visualisation
    * graph construction summary graphs
        * can include embedding information
    * gnn layer switching summary graphs
    * gnn summary graph which doesn't include switching info. just layers, types, sizes. like cdn diagram
     * make fine tuning the contextual embedder optional

* render sparse adjacency matrices


* Make bert tokeniser cased
* add in returning edges for the directed edges

NLP Libs:
Spacy - ineffective
StanfordNLP python wrapper has no coref


Installs:
ENV_NAME=gnn_env
y
conda create --name ${ENV_NAME} python=3.6
conda activate ${ENV_NAME}

CUDA=cu100
TORCH=1.6.0
pip install torch==${TORCH}+${CUDA} torchvision==0.7.0+${CUDA} -f https://download.pytorch.org/whl/torch_stable.html --user --no-cache-dir
pip install torch-scatter==latest+${CUDA} -f https://pytorch-geometric.com/whl/torch-${TORCH}.html --user --no-cache-dir
pip install torch-sparse==latest+${CUDA} -f https://pytorch-geometric.com/whl/torch-${TORCH}.html --user --no-cache-dir
pip install torch-cluster==latest+${CUDA} -f https://pytorch-geometric.com/whl/torch-${TORCH}.html --user --no-cache-dir
pip install torch-spline-conv==latest+${CUDA} -f https://pytorch-geometric.com/whl/torch-${TORCH}.html --user --no-cache-dir
pip install torch-geometric --user
pip install transformers --user
pip install -U spacy==2.1.3 --user
pip install neuralcoref --no-binary neuralcoref --user
python -m spacy download en_core_web_sm

